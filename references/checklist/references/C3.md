# C3 · 网址归一(P0)

## 介绍

目的:让每个页面只有一个 URL 身份。www/裸域 × http/https × 尾斜杠能把同一页拼出多个地址,不归一的后果分三层:

1. **抓取重复** —— 引擎把变体当不同页面各自抓,crawl budget 花在重复内容上,新页反而抓得慢;
2. **权重分裂** —— 外链有人链 www、有人链裸域,link equity 记在不同变体上,合起来能排前面的页,分开谁都排不上;
3. **下游失去基准** —— C8 的 canonical、sitemap 的 URL、hreflang 的互指都要求写「那个唯一的 URL」,没有归一就没有「那个」。

**需真实域名才能测**(DNS/证书层),本地部署时 checker 记 N.A.。

## 实现指导

- server / CDN **一处配置**:全部变体 301 到唯一 canonical host;选定三件事——www 或裸域、强制 https、尾斜杠约定
- 变体必须**一跳直达**最终形态(同时受 C20 约束),别 http→https→www 链式:

```nginx
# 示例(仅供参考;nginx 写法,CDN 规则同理)
server {
    listen 80;  listen 443 ssl;
    server_name tigerless.com;              # 裸域(含 http)
    return 301 https://www.tigerless.com$request_uri;   # 一跳直达最终形态
}
```

- 尾斜杠:选定一种(建议无尾斜杠),另一种 301 过去,框架层配置(Next `trailingSlash`、nginx `rewrite`)
- 自查:`curl -I` 四变体(http/https × www/裸域),全部 301 且 Location 一致、hop=1

## 权威参考

- [Google:URL 归一与重复合并](https://developers.google.com/search/docs/crawling-indexing/canonicalization) — 变体如何被引擎处理的权威说明
- [Google:重定向与搜索](https://developers.google.com/search/docs/crawling-indexing/301-redirects)
